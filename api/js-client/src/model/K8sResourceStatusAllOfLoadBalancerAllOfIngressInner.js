/**
 * K8S Resource API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import K8sResourceStatusAllOfLoadBalancerAllOfIngressInnerAllOfPortsInner from './K8sResourceStatusAllOfLoadBalancerAllOfIngressInnerAllOfPortsInner';

/**
 * The K8sResourceStatusAllOfLoadBalancerAllOfIngressInner model module.
 * @module model/K8sResourceStatusAllOfLoadBalancerAllOfIngressInner
 * @version 1.0.0
 */
class K8sResourceStatusAllOfLoadBalancerAllOfIngressInner {
    /**
     * Constructs a new <code>K8sResourceStatusAllOfLoadBalancerAllOfIngressInner</code>.
     * @alias module:model/K8sResourceStatusAllOfLoadBalancerAllOfIngressInner
     */
    constructor() { 
        
        K8sResourceStatusAllOfLoadBalancerAllOfIngressInner.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>K8sResourceStatusAllOfLoadBalancerAllOfIngressInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/K8sResourceStatusAllOfLoadBalancerAllOfIngressInner} obj Optional instance to populate.
     * @return {module:model/K8sResourceStatusAllOfLoadBalancerAllOfIngressInner} The populated <code>K8sResourceStatusAllOfLoadBalancerAllOfIngressInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new K8sResourceStatusAllOfLoadBalancerAllOfIngressInner();

            if (data.hasOwnProperty('hostname')) {
                obj['hostname'] = ApiClient.convertToType(data['hostname'], 'String');
            }
            if (data.hasOwnProperty('ip')) {
                obj['ip'] = ApiClient.convertToType(data['ip'], 'String');
            }
            if (data.hasOwnProperty('ports')) {
                obj['ports'] = ApiClient.convertToType(data['ports'], [K8sResourceStatusAllOfLoadBalancerAllOfIngressInnerAllOfPortsInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>K8sResourceStatusAllOfLoadBalancerAllOfIngressInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>K8sResourceStatusAllOfLoadBalancerAllOfIngressInner</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['hostname'] && !(typeof data['hostname'] === 'string' || data['hostname'] instanceof String)) {
            throw new Error("Expected the field `hostname` to be a primitive type in the JSON string but got " + data['hostname']);
        }
        // ensure the json data is a string
        if (data['ip'] && !(typeof data['ip'] === 'string' || data['ip'] instanceof String)) {
            throw new Error("Expected the field `ip` to be a primitive type in the JSON string but got " + data['ip']);
        }
        if (data['ports']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ports'])) {
                throw new Error("Expected the field `ports` to be an array in the JSON data but got " + data['ports']);
            }
            // validate the optional field `ports` (array)
            for (const item of data['ports']) {
                K8sResourceStatusAllOfLoadBalancerAllOfIngressInnerAllOfPortsInner.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * hostname is set for load-balancer ingress points that are DNS based.
 * @member {String} hostname
 */
K8sResourceStatusAllOfLoadBalancerAllOfIngressInner.prototype['hostname'] = undefined;

/**
 * ip is set for load-balancer ingress points that are IP based.
 * @member {String} ip
 */
K8sResourceStatusAllOfLoadBalancerAllOfIngressInner.prototype['ip'] = undefined;

/**
 * ports provides information about the ports exposed by this LoadBalancer.
 * @member {Array.<module:model/K8sResourceStatusAllOfLoadBalancerAllOfIngressInnerAllOfPortsInner>} ports
 */
K8sResourceStatusAllOfLoadBalancerAllOfIngressInner.prototype['ports'] = undefined;






export default K8sResourceStatusAllOfLoadBalancerAllOfIngressInner;

