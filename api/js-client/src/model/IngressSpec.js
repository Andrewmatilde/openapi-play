/**
 * K8S Resource API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import IngressSpecAllOfDefaultBackend from './IngressSpecAllOfDefaultBackend';
import IngressSpecAllOfRulesInner from './IngressSpecAllOfRulesInner';
import IngressSpecAllOfTlsInner from './IngressSpecAllOfTlsInner';

/**
 * The IngressSpec model module.
 * @module model/IngressSpec
 * @version 1.0.0
 */
class IngressSpec {
    /**
     * Constructs a new <code>IngressSpec</code>.
     * spec is the desired state of the Ingress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
     * @alias module:model/IngressSpec
     */
    constructor() { 
        
        IngressSpec.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>IngressSpec</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IngressSpec} obj Optional instance to populate.
     * @return {module:model/IngressSpec} The populated <code>IngressSpec</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IngressSpec();

            if (data.hasOwnProperty('defaultBackend')) {
                obj['defaultBackend'] = IngressSpecAllOfDefaultBackend.constructFromObject(data['defaultBackend']);
            }
            if (data.hasOwnProperty('ingressClassName')) {
                obj['ingressClassName'] = ApiClient.convertToType(data['ingressClassName'], 'String');
            }
            if (data.hasOwnProperty('rules')) {
                obj['rules'] = ApiClient.convertToType(data['rules'], [IngressSpecAllOfRulesInner]);
            }
            if (data.hasOwnProperty('tls')) {
                obj['tls'] = ApiClient.convertToType(data['tls'], [IngressSpecAllOfTlsInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IngressSpec</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IngressSpec</code>.
     */
    static validateJSON(data) {
        // validate the optional field `defaultBackend`
        if (data['defaultBackend']) { // data not null
          IngressSpecAllOfDefaultBackend.validateJSON(data['defaultBackend']);
        }
        // ensure the json data is a string
        if (data['ingressClassName'] && !(typeof data['ingressClassName'] === 'string' || data['ingressClassName'] instanceof String)) {
            throw new Error("Expected the field `ingressClassName` to be a primitive type in the JSON string but got " + data['ingressClassName']);
        }
        if (data['rules']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rules'])) {
                throw new Error("Expected the field `rules` to be an array in the JSON data but got " + data['rules']);
            }
            // validate the optional field `rules` (array)
            for (const item of data['rules']) {
                IngressSpecAllOfRulesInner.validateJSON(item);
            };
        }
        if (data['tls']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tls'])) {
                throw new Error("Expected the field `tls` to be an array in the JSON data but got " + data['tls']);
            }
            // validate the optional field `tls` (array)
            for (const item of data['tls']) {
                IngressSpecAllOfTlsInner.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {module:model/IngressSpecAllOfDefaultBackend} defaultBackend
 */
IngressSpec.prototype['defaultBackend'] = undefined;

/**
 * ingressClassName is the name of an IngressClass cluster resource. Ingress controller implementations use this field to know whether they should be serving this Ingress resource, by a transitive connection (controller -> IngressClass -> Ingress resource). Although the `kubernetes.io/ingress.class` annotation (simple constant name) was never formally defined, it was widely supported by Ingress controllers to create a direct binding between Ingress controller and Ingress resources. Newly created Ingress resources should prefer using the field. However, even though the annotation is officially deprecated, for backwards compatibility reasons, ingress controllers should still honor that annotation if present.
 * @member {String} ingressClassName
 */
IngressSpec.prototype['ingressClassName'] = undefined;

/**
 * rules is a list of host rules used to configure the Ingress. If unspecified, or no rule matches, all traffic is sent to the default backend.
 * @member {Array.<module:model/IngressSpecAllOfRulesInner>} rules
 */
IngressSpec.prototype['rules'] = undefined;

/**
 * tls represents the TLS configuration. Currently the Ingress only supports a single TLS port, 443. If multiple members of this list specify different hosts, they will be multiplexed on the same port according to the hostname specified through the SNI TLS extension, if the ingress controller fulfilling the ingress supports SNI.
 * @member {Array.<module:model/IngressSpecAllOfTlsInner>} tls
 */
IngressSpec.prototype['tls'] = undefined;






export default IngressSpec;

